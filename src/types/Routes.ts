export enum MainViewRoutes {
  cabinets = "cabinets",
  institutions = "institutions",
  items = "items",
  users = "users"
}

export enum RoutesEnum {
  main = "/",
  auth = "auth",
  logout = "logout",
  signIn = "signin",
  profile = "profile",
  view = "view",
  noAccess = "no-access",
  all = "*"
}
